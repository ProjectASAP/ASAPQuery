query_groups:
  - id: 1
    queries:
      - "rate(http_requests_total[5m])"
    repetition_delay: 300
    controller_options:
      accuracy_sla: 0.99
      latency_sla: 1.0
aggregate_cleanup:
  policy: "read_based"
