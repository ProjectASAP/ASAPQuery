query_groups:
  - id: 1
    queries:
      - "quantile by (job) (0.99, http_requests_total)"
    repetition_delay: 60
    controller_options:
      accuracy_sla: 0.99
      latency_sla: 1.0
aggregate_cleanup:
  policy: "read_based"
