See `AGENTS.md` for process conventions and the code design / test coverage
checklists — it applies to Claude Code the same as any other agent. The
`code-design-review` and `test-coverage-review` skills below wrap the same
checklists for Claude Code's auto-loading.
